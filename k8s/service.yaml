apiVersion: v1
kind: Service
metadata:
  name: llm-compression
  labels:
    app: llm-compression
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: llm-compression
